version: '3.4'
services:
  nginx:
    image: nginx:latest
    restart: always
    depends_on:
      - mariadb

  webtrees:
    build: webtrees
    restart: always
    depends_on:
      - mariadb

  mariadb:
    image: mariadb:10.2
    restart: always
